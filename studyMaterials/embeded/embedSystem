embed s/m:
 combo of bth h/w,s/w,
 either independent s/m or a part of large s/m
 having dedicated h/w,s/w to perform specific fn
 EX:ATM,digital camera,automatic washing machine,dishwasher,printer

diff b/w computer and embed s/m
 computer-general purpose(like design app or s/w)

components:
 h/w components-timer,i/o port,keyboard,monitor,CPU
 s/w components
 os (RTOs)

 normal os:
  -performance imp,whtever time it takes
 realtime OS:
  -giving more imp to time,time bounded
 hard OS-having very critical deadline
 soft OS- Multi-desktop user interface and a Multi-workspace
