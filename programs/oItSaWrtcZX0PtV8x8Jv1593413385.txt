#include <stdio.h>
#include <stdlib.h>
int gcd(int a,int b)
{
    while(a!=b)
    {
        if(a>b)
            return gcd(a-b,a);
        else
            return gcd(a,b-a);

    }
    return a;
}

int main()
{
    int a=81,b=153;
   printf("%d", gcd(a,b));
    return 0;
}
