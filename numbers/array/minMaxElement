#include<stdio.h>
#include<string.h>
int main()
{
   int n=10,min,max,i;
   int a[10]={56,21,34,9,100,8,6,23,54,28};
   min=a[0],max=a[0];
   for(i=0;i<n;i++)
   {
    if(min>a[i])
        min=a[i];
    if(max<a[i])
        max=a[i];
   }
printf("min is %d and max is %d",min,max);
}
