#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
void reverse(int a[],int start,int end)
{
        int temp;
        while(start<=end)
        {
            temp=a[start];
            a[start]=a[end];
            a[end]=temp;
            start++;
            end--;
        }
}
int main()
{
    int n;
    scanf("%d",&n);
    int a[n],i;
    for(i=0;i<n;i++)
        scanf("%d ",&a[i]);
    int rotation;
    scanf("%d ",&rotation);

    reverse(a,0,rotation-1);
    reverse(a,rotation,n-1);
    reverse(a,0,n-1);

    for(i=0;i<n;i++)
        printf("%d ",a[i]);
    return 0;
}
