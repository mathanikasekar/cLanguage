//36=1,2,3,4,6,9,12,18,36
 the factors of 36 are mentioned above
 to optimise the code we separate factors into 2 sets
 one set consists of factors <=sqrt(i/p) i.e,1,2,3,4,6 and store it in separate array say a[100]
 other set consists of factors >=sqrt(i/p) i.e,6,9,12,18,36 and store it in separate array say b[100]
 here if the number has exact sqrt thn it wil be stored in both arrays
  so we store >sqrt(i/p) in other set
 and we get values in b[100] array by values in a[100]
 1*36=36   36/1=36
 2*18=36   36/2=18
 3*12=36   36/3=12
 4*9=36    36/2=18
 6*6=36    36/6=6

#include<stdio.h>
int main()
{
    long long int n,k,i,a[100]={0},b[100]={0},bvalue,acount=0,bcount=0;
    n=36,k=6;
    for(i=1;i*i<=n;i++)
    {
        if(n%i==0)
        {
            a[acount]=i;
        bvalue=n/a[acount];
        if(a[acount]!=bvalue)//to avoid sqrt of i/p in both arrays
        {
        b[bcount]=bvalue;
        bcount++;
        }
        acount++;
    }
    }
    long long int total=acount+bcount;
    if(k>total)
        printf("1");
    else if(k<=bcount)
    {
        printf("%lld",b[k-1]);
    }
    else
    {
        k=k-bcount;
         printf("%lld",a[acount-k]);
    }
    /*for(i=0;i<acount;i++)
    {

        printf("%lld ",a[i]);
    }
    printf("\n");
    for(i=0;i<bcount;i++)
    {
        printf("%lld ",b[i]);
    }*/
}
