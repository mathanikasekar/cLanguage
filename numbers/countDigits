#include<stdio.h>
#include<string.h>
int main()
{
  int num=2134,i,count;
  for(i=0 ; num ; num/=10,i++) //loops starts with 0
    count=i;
  printf("%d",count+1);    //must add 1 because loop starts from 0
  return 0;
}
