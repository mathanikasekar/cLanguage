#include<stdio.h>
#include<malloc.h>
#include<string.h>
int main()
{
    char *s;
    s=(char *)malloc(sizeof(char));
    printf("Enter i/p");
    scanf("%s",s);
    printf("o/p is ");
    printf("%s",s);
}
